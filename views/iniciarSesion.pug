<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login 
    .contenedor-formulario
      h1 Iniciar Sesión en UpTask
      //- if mensajes
      //-   p.alerta.error= mensajes
      if locals.mensajes
        .alertas
          each categoria in Object.keys(locals.mensajes)
            each error in mensajes[categoria]
              .alerta(class=`${categoria}`)
                p.alerta(class=`${categoria}`)= error

      form#formulario.caja.caja-login(action='/iniciar-sesion' method='POST')
        .campo
          label(for='email') E-mail:
          input(type="email" name='email' placeholder='Ingresa tu cuenta de E-mail' value= email)
        .campo
          label(for='password') Password:
          input(type="password" name='password' placeholder='Ingresa tu password' value= password)
        .campo.enviar
          input.boton(type='submit' value='Iniciar Sesión')
        .campo.acciones
          a(href="/crear-cuenta") ¿Aun no tienes cuenta? Crea una Gratis
          a(href="/reestablecer") ¿Olvidaste tu contraseña?
